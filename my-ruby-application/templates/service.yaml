apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-ruby-application.fullname" . }}
  labels:
    {{- include "my-ruby-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-ruby-application.selectorLabels" . | nindent 4 }}
